@model IEnumerable<CategoryVM>

<h2>Category List</h2>

<p>
    <a asp-controller="Category" asp-action="CreateCategory">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th style="width:10%">Id</th>
            <th>CategoryName</th>
            <th style="width:20%"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Id
                </td>
                <td>
                    @item.CategoryName
                </td>
                <td>
                    <a asp-controller="Category" asp-action="CategoryDetails" asp-route-id="@item.Id" class="d-none d-sm-inline-flex btn btn-sm btn-primary shadow-sm">
                        Details
                    </a>
                    <a asp-controller="Category" asp-action="Edit" asp-route-id="@item.Id" class="d-none d-sm-inline-flex btn btn-sm btn-primary shadow-sm">
                        Edit
                    </a>
                    <a asp-controller="Category" asp-action="Delete" asp-route-id="@item.Id" class="d-none d-sm-inline-flex btn btn-sm btn-danger shadow-sm">
                        Delete
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>